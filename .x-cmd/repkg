

action_main(){
    local download_fp
    # download ...

    x uz "$download_fp"
    x cd "$download_fp"

    local folder
    for folder in pages.*; do
        local dstfp="$folder.tgz"
        [ -f "$dstfp" ] || x z "$dstfp" "$folder"
    done
}

action_main "$@"
